---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PrivateClientPage from '@/components/PrivateClientPage.astro';
import { pizzaWorldBransonJulyBillingReview as page } from '@/data/clientPrivatePages';
---

<BaseLayout title={page.title} description={page.description} canonicalPath={page.canonicalPath} noindex section="private">
  <PrivateClientPage page={page} />
</BaseLayout>
